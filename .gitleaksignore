# Findings gitleaks is told to stay quiet about, one line each, by fingerprint.
# A fingerprint names one match in one commit in one file: nothing else here is
# exempted, and a NEW secret - including a new one in this same .env - still
# fails the run.
#
# What is listed below is already known and already written down: the README's
# Security notes name every credential in the tracked .env and give the order
# for untracking it without deleting it from the running host. Left red
# forever, this scan would stop being able to report the next leak, which is
# the only thing it can still usefully do about a value that is already in the
# history.

bf0f6d124fe05e079b21f2383d2712af92cdf429:.env:generic-api-key:22
